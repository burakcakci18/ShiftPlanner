<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true" throwExceptions="false">

	<variable name="logDirectory" value="C:\ProjectLogs\ShiftPlanner\logs" />
	<variable name="archiveDirectory" value="C:\ProjectLogs\ShiftPlanner\logs\archive" />

	<targets>
		<target name="logfile" xsi:type="File"
						fileName="${var:logDirectory}\${var:serviceName}.log"
						layout="${longdate} | ${level:uppercase=true} | ${logger} | ${message} ${exception:format=toString,Data:maxInnerExceptionLevel=5}"
						archiveFileName="${var:archiveDirectory}\${var:serviceName}.{#}.log"
						archiveAboveSize="10485760"
						maxArchiveFiles="10" />

		<target name="logconsole" xsi:type="Console" />
	</targets>

	<rules>
		<logger name="*" minlevel="Trace" writeTo="logfile,logconsole" />
	</rules>
</nlog>